import Form from "~/components/Form";

const page = ({}) => {
  return (
    <div className="w-full mt-[10vh] md:mt-0 md:h-screen grid place-content-center">
      <Form orientation={"portrait"} />
    </div>
  );
};

export default page;
